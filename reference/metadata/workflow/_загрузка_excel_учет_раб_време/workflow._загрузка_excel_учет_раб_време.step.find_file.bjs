var fileData = await from("operation.учет_раб_врем_ауп.attached_files")
  .select(['uid', 'filename'])
  .where("objectuid = @id")
  .parameter("id", _parameters.number, 11)
  .query();

return fileData;