var fileData = await from("operation.монтаж_мк_нов.attached_files")
  .select(['uid', 'filename'])
  .where("objectuid = @objectuid")
  .parameter("objectuid", _parameters.number, 11)
  .query();

return fileData;