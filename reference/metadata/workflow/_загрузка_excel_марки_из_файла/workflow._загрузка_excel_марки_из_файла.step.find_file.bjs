var fileData = await from("operation.загрузка_марки_новые.attached_files")
  .select(['uid', 'filename'])
  .where("objectuid = @id")
  .parameter("id", _parameters.number, 11)
  .query();

return fileData;