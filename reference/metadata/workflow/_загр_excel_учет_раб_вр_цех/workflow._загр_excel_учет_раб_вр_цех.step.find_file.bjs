var fileData = await from("operation.учет_раб_времени_цех.attached_files")
  .select(['uid', 'filename'])
  .where("objectuid = @id")
  .parameter("id", _parameters.number, 11)
  .query();

return fileData;