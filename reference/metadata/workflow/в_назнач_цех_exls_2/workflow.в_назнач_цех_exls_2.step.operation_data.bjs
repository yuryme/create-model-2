var result = await from('operation.цеха_марка')
  .select(['number', 'date', 'проект_заказ'])
  .where('number = @number')
  .parameter('number', _parameters.number, 11)
  .getDisplays()
  .query();

return result;