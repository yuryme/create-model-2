var result = await from('operation.пто_ид')
  .select(['number', 'date', 'проект_заказ', 'шифр_км'])
  .where('opr_пто_ид.number = @number')
  .parameter('number', _parameters.number, 11)
  .query();

return result;