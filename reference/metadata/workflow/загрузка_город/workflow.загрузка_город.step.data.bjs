var table = createTable([{name: 'id', dataType: 'number'} 
                         ,{name: 'uid'}
                         ,{name: 'title'}
                         ]);

_data.request.data.forEach( row => {
  table.addRow([row.id, 
                row.uid,
                row.title
                ]);
});

return table;