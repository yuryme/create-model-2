var fileData = await from("operation.обновление_марок.attached_files")
  .select(['uid', 'filename'])
  .where("objectuid = @id")
  .parameter("id", _parameters.number, 11)
  .query();

return fileData;