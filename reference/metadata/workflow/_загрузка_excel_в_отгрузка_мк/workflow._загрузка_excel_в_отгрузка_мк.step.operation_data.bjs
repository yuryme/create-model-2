var operationData = await from('operation.отгрузка')
  .select(['number', 'date', 'проектзаказ as проект_заказ'])
  .where('opr_отгрузка.number = @number')
  .parameter('number', 3)
  .getDisplays()
  .query();

return operationData;