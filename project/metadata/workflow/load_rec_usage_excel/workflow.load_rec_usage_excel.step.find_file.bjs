var fileData = await from('operation.recognition_usage.attached_files')
  .select(['uid', 'filename'])
  .where('objectuid = @id')
  .parameter('id', _parameters.id, 11)
  .query();

return fileData;
